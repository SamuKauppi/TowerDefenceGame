public enum SpawnOrder
{
    // Spawn every enemy in one array element before moving to next
    AllFromOne,
    // Spawn one enemy randomly from array elements
    Random,
    // Spawn one enemy from array element before moving to next one
    Rotating
}
